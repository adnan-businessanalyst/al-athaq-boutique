# PostgreSQL connection string (required for Prisma migrate / seed / production)
# Examples:
#   postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public
#   Replit: use the connection string from your Replit Postgres / Neon / Supabase DB
DATABASE_URL="postgresql://user:password@localhost:5432/al_athaq?schema=public"

# Public site URL for canonical links, sitemap, Open Graph
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
